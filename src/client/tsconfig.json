{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "lib": ["ESNext", "DOM", "DOM.Iterable", "DOM.AsyncIterable"],
        "types": [],
        "composite": true
    },
    "include": ["../shared/"]
}
